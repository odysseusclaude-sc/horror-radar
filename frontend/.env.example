# API base URL — set this to your backend server address in production
# In development, Vite proxies /api to localhost:8000 (see vite.config.ts)
# For Vercel, set VITE_API_URL in the Vercel dashboard environment variables
VITE_API_URL=/api
